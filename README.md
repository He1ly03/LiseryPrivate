# LiseryPrivate

![Version](https://img.shields.io/badge/version-1.2-blue.svg)
![Minecraft](https://img.shields.io/badge/minecraft-1.21-green.svg)
![License](https://img.shields.io/badge/license-MIT-orange.svg)

**LiseryPrivate** — современный плагин для приватности чанков на Minecraft серверах. Предоставляет игрокам возможность приватизировать территорию, управлять доверенными лицами, продавать чанки на аукционе и многое другое.

## ✨ Основные возможности

- 🔒 **Система привата чанков** — игроки могут приватизировать чанки за деньги
- 👥 **Доверенные лица** — добавление других игроков с правами на ваш чанк
- 💰 **Интеграция с экономикой** — поддержка Vault и CoinsEngine
- 🛒 **Система аукциона** — продажа и покупка чанков через аукцион
- 🎨 **Голограммы** — визуальное отображение информации о чанках (Text Display Entity)
- 📋 **Меню-система** — удобные GUI-меню для управления чанками (LiseryMenu)
- 🛡️ **Защита от разрушения** — настраиваемая защита от различных действий
- 🎯 **Визуальные границы** — частицы по границам чанков при использовании редактора
- 📊 **PlaceholderAPI** — интеграция для отображения данных в других плагинах
- 🔄 **Автоматическая проверка обновлений** — уведомления о новых версиях
- ⚙️ **Гибкие настройки** — детальная конфигурация через YAML файлы

## 📋 Требования

### Обязательные зависимости:
- **Paper/Spigot** 1.21+
- **WorldGuard** 7.0+
- **WorldEdit** 7.3+

### Опциональные зависимости:
- **Vault** — для интеграции с экономическими плагинами
- **LuckPerms** — для лимитов чанков по группам
- **PlaceholderAPI** — для плейсхолдеров
- **CoinsEngine** — альтернативная система экономики

## 🚀 Установка

1. Скачайте последнюю версию плагина из релизов
2. Поместите файл `LiseryPrivate.jar` в папку `plugins/` вашего сервера
3. Убедитесь, что установлены **WorldGuard** и **WorldEdit**
4. Запустите сервер (плагин создаст необходимые конфигурационные файлы)
5. Настройте конфигурацию в `plugins/LiseryPrivate/config.yml`
6. Перезагрузите плагин командой `/chunk admin reload`

## 📖 Команды

### Основные команды:

| Команда | Описание | Права |
|---------|----------|-------|
| `/chunk` или `/c` или `/private` | Показать справку | `liseryprivate.use` |
| `/chunk wand` | Получить редактор регионов | `liseryprivate.wand` |
| `/chunk info` | Информация о текущем чанке | `liseryprivate.use` |
| `/chunk private [название]` | Приватить текущий чанк | `liseryprivate.use` |
| `/chunk unprivate` | Расприватить чанк | `liseryprivate.use` |
| `/chunk rename <старое> <новое>` | Переименовать чанк | `liseryprivate.use` |
| `/chunk trust <игрок>` | Добавить игрока в доверенные | `liseryprivate.use` |
| `/chunk untrust <игрок>` | Убрать игрока из доверенных | `liseryprivate.use` |
| `/chunk menu` | Открыть меню управления | `liseryprivate.use` |
| `/chunk settings <флаг>` | Настройки защиты чанка | `liseryprivate.use` |
| `/chunk auction` | Открыть аукцион чанков | `liseryprivate.use` |
| `/chunk sell <цена>` | Выставить чанк на продажу | `liseryprivate.use` |
| `/chunk unsell` | Убрать чанк с продажи | `liseryprivate.use` |

### Административные команды:

| Команда | Описание | Права |
|---------|----------|-------|
| `/chunk admin reload` | Перезагрузить плагин | `liseryprivate.admin` |
| `/chunk admin forceunprivate` | Принудительно расприватить чанк | `liseryprivate.admin` |

## 🔐 Права доступа

| Право | Описание | По умолчанию |
|-------|----------|--------------|
| `liseryprivate.use` | Использование основных функций | `true` |
| `liseryprivate.wand` | Использование редактора регионов | `true` |
| `liseryprivate.admin` | Административные функции | `op` |

**Примечание:** Операторы по умолчанию имеют неограниченное количество приватных чанков.

## ⚙️ Конфигурация

### Основные настройки (`config.yml`)

```yaml
# Главная команда плагина
settings:
  main-command: "chunk"

# Экономика
economy:
  chunk-private: 100.0          # Цена привата чанка
  chunk-unprivate: 50.0         # Возврат при распривате
  auction-enabled: true         # Включен ли аукцион
  min-chunk-price: 100.0        # Минимальная цена на аукционе
  max-chunk-price: 10000.0      # Максимальная цена на аукционе

# Лимиты чанков
limits:
  default: 3                    # Стандартный лимит
  groups:                       # Лимиты для групп LuckPerms
    vip: 5
    premium: 10

# Защита чанков
protection:
  protect-build: true           # Защита от строительства
  protect-destroy: true         # Защита от разрушения
  protect-use: true             # Защита от использования блоков
  protect-switch: true          # Защита от переключения (рычаги и т.д.)
  protect-mobs: true            # Защита мобов
  protect-pvp: true             # Защита от PvP
  protect-fire: true            # Защита от огня
  protect-explosion: true       # Защита от взрывов

# Хранение данных
storage:
  type: SQLITE                  # SQLITE или MYSQL
  sqlite:
    file: chunkprivate.db
  mysql:
    host: localhost
    port: 3306
    database: chunkprivate
    username: root
    password: password
    use-ssl: false
    table-prefix: cp_

# Проверка обновлений
update-checker:
  enabled: true
  repository: "He1ly03/LiseryPrivate"
  notify-operators: true
  debug: false
```

### Настройки голограмм (`holograms.yml`)

```yaml
# Высота голограммы над центром чанка
hologram-height: 3.0

# Настройки отображения
text-entity-settings:
  billboard: center              # Тип биллборда
  background-color: "#C8000000"  # Цвет фона (#AARRGGBB)
  scale: 1.5                     # Масштаб

# Голограммы для разных типов чанков
holograms:
  free-chunk:                    # Свободный чанк
    enabled: true
    region-editor-display-only: true
    lines:
      - "&6Чанк свободен"
      - "&7Цена: &e%chunkprivate_price%"
  claimed-chunk:                 # Заприваченный чанк
    enabled: true
    region-editor-display-only: true
    lines:
      - "<gold>%owner%</gold>"
      - "&7- %pvp% &7-"
  for-sale-chunk:                # Чанк на продаже
    enabled: true
    region-editor-display-only: false
    lines:
      - "<gold>$ <white>Продажа</white> $</gold>"
      - "&aЦена: &e%price%"
```

**Примечание:** Голограммы динамически отображаются над головой игрока в центре чанка на указанной высоте.

## 📊 PlaceholderAPI

Плагин предоставляет следующие плейсхолдеры:

| Плейсхолдер | Описание |
|-------------|----------|
| `%liseryprivate_chunk%` | Имя привата |
| `%liseryprivate_owner%` | Имя владельца |
| `%liseryprivate_index%` | Номер привата владельца |
| `%liseryprivate_x%` | X координата чанка |
| `%liseryprivate_z%` | Z координата чанка |
| `%liseryprivate_world%` | Название мира |
| `%liseryprivate_price%` | Цена чанка |
| `%liseryprivate_pvp%` | Статус PVP (Включено/Отключено) |
| `%liseryprivate_is_claimed%` | `true/false` если чанк приватен |
| `%liseryprivate_is_owner%` | `true/false` если игрок владелец |
| `%liseryprivate_is_trusted%` | `true/false` если игрок доверенный |

**Использование:** Установите PlaceholderAPI и используйте плейсхолдеры в любом плагине, который поддерживает PlaceholderAPI (например, в сообщениях, меню, чате и т.д.).

## 🎨 Меню

Плагин использует систему меню **LiseryMenu** для удобного управления чанками:

- **Меню приватности** (`/chunk menu`) — основное меню для управления чанками
- **Меню доверенных лиц** — управление списком доверенных игроков
- **Меню настроек** — настройка защиты чанка
- **Меню аукциона** — просмотр и покупка чанков на аукционе
- **Меню продаж** — список ваших чанков на продаже

Все меню настраиваются через файлы в папке `plugins/LiseryPrivate/menu/`. Подробная документация находится в файле `menu/README.md`.

## 🛡️ Система защиты

Плагин предоставляет следующие типы защиты:

1. **Защита от строительства** — предотвращает установку блоков
2. **Защита от разрушения** — предотвращает разрушение блоков
3. **Защита от использования** — блокирует использование сундуков, печей и т.д.
4. **Защита от переключения** — блокирует использование рычагов, кнопок, пластин
5. **Защита мобов** — предотвращает атаку мобов на чанке
6. **Защита от PvP** — отключает PvP на чанке
7. **Защита от огня** — предотвращает поджог
8. **Защита от взрывов** — защищает от TNT и динамита (криперы всегда работают)

Все настройки защиты можно изменить как глобально в `config.yml`, так и индивидуально для каждого чанка через `/chunk settings` или меню.

**Исключения:** Владелец чанка и доверенные лица могут выполнять любые действия независимо от настроек защиты.

## 🎯 Редактор регионов

Редактор регионов — специальный предмет, который позволяет:

- **Визуализировать границы** чанков с помощью частиц
- **Просматривать голограммы** с информацией о чанках
- **Управлять чанками** через контекстное меню

Получить редактор: `/chunk wand`

**Настройки редактора** (`config.yml`):
```yaml
wand-item:
  item: BREEZE_ROD              # Предмет редактора
  protect-use: true             # Защита от использования
  name: "&aРедактор регионов"   # Название предмета
  lore:                         # Описание предмета
    - "&7Редактирование регионов"
```

## 🔄 Обновления

Плагин автоматически проверяет наличие обновлений при запуске сервера. Для настройки проверки обновлений:

```yaml
update-checker:
  enabled: true
  repository: "He1ly03/LiseryPrivate"  # GitHub репозиторий
  notify-operators: true                # Уведомлять операторов
  debug: false                          # Режим отладки
```

При наличии новой версии:
- В консоли появится уведомление
- Операторы получат кликабельное сообщение в игре со ссылкой на скачивание

## 📁 Структура файлов

```
plugins/LiseryPrivate/
├── config.yml              # Основной конфигурационный файл
├── messages.yml            # Сообщения плагина
├── holograms.yml           # Настройки голограмм
├── integrations.yml        # Настройки интеграций
├── chunkprivate.db         # База данных (SQLite)
└── menu/                   # Файлы меню
    ├── private.yml
    ├── trust.yml
    ├── settings.yml
    ├── sell.yml
    ├── sell-list.yml
    ├── confirm.yml
    ├── action.txt          # Документация действий
    └── README.md           # Документация меню
```

## 🐛 Отладка

Для включения режима отладки:

1. Установите `update-checker.debug: true` в `config.yml` для отладки проверки обновлений
2. Проверьте консоль сервера на наличие ошибок
3. Убедитесь, что все зависимости установлены корректно

## 📝 Сообщения

Все сообщения плагина настраиваются в файле `messages.yml`. Вы можете:
- Изменить текст сообщений
- Настроить префикс плагина
- Добавить цвета и форматирование

Поддерживаются плейсхолдеры:
- `%player%` — имя игрока
- `%owner%` — владелец чанка
- `%chunk%` — название чанка
- `%price%` — цена
- `%refund%` — возвращаемая сумма
- И другие (см. `messages.yml`)

## 🔌 API

Плагин предоставляет API для разработчиков:

```java
// Получить экземпляр плагина
LiseryPrivate plugin = LiseryPrivate.getInstance();

// Получить менеджер чанков
ChunkManager chunkManager = plugin.getChunkManager();

// Получить данные чанка
ChunkData chunkData = chunkManager.getChunkData(chunk);
```

## 📄 Лицензия

Этот проект распространяется под лицензией MIT.

## 👨‍💻 Автор

**He1ly03**

## 🤝 Поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте документацию в папке `menu/README.md`
2. Убедитесь, что все зависимости установлены
3. Проверьте версию сервера (требуется 1.21+)
4. Включите режим отладки и проверьте логи

## 📌 Changelog

### Версия 1.2
- ✅ Добавлена система проверки обновлений
- ✅ Улучшена система голограмм (динамическое позиционирование)
- ✅ Полная поддержка Base64 текстур в меню
- ✅ Миграция на систему меню LiseryMenu
- ✅ Интеграция с PlaceholderAPI
- ✅ Поддержка операторов с неограниченным количеством чанков
- ✅ Исправлены баги с отображением голов в меню

### Версия 1.1
- ✅ Базовая функциональность привата чанков
- ✅ Система аукциона
- ✅ Меню управления

---

**Спасибо за использование LiseryPrivate!** 🎉

